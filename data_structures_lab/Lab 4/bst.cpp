#include <iostream>

using namespace std;

struct node {
	int value;
	node *left, *right;
	node(v) {
		value = v;
		left = right = NULL;	
	}
};

void insertNode(node *tree, node *n) {
	node *t = tree;
	
}

node *createTree(int a[], int N) {
	node *tree = NULL;
	for(int i = 0; i < N; i++) {
		
	}
}
